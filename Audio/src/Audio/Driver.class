package sample;

import javafx.application.Application;
import javafx.stage.Stage;

public class Controller extends Application {
    
    public static void main(String[] args) throws InterruptedException {
        
        //Default constructor, issues with parameterized constructor
        AudioUtility audioUtility = new AudioUtility();
        
        //Provide the absolute path of file
        audioUtility.play("D:\\Music\\Coldplay - Paradise (Official Video).mp3"); 
        
        //For testing purpose only
        /*for(int i=1;i<=10;i++){
            if(i%2==1)
                audioUtility.mute();
            else
                audioUtility.setVolume(100);
            Thread.sleep(2000);
        }*/
    }

    @Override
    public void start(Stage primaryStage) throws Exception {
    }
}
